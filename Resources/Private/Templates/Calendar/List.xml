<f:layout name="Feed" />

<f:section name="items">
<f:for each="{indices}" as="index">
	<f:render partial="{index.configuration.partialIdentifier}/Feed/Item" arguments="{index:index}" optional="true" />
</f:for>
</f:section>
